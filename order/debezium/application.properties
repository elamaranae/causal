# Sink - RabbitMQ (AMQP)
debezium.sink.type=rabbitmq
debezium.sink.rabbitmq.connection.host=causal-order-rabbitmq
debezium.sink.rabbitmq.connection.port=5672
debezium.sink.rabbitmq.connection.username=guest
debezium.sink.rabbitmq.connection.password=guest
debezium.sink.rabbitmq.ackTimeout=30000
debezium.sink.rabbitmq.routingKey=order.events
debezium.sink.rabbitmq.exchange=order.events
debezium.sink.rabbitmq.deliveryMode=2
debezium.sink.rabbitmq.null.value=null

# Source - PostgreSQL
debezium.source.connector.class=io.debezium.connector.postgresql.PostgresConnector
debezium.source.database.hostname=causal-order-db
debezium.source.database.port=5432
debezium.source.database.user=postgres
debezium.source.database.password=password
debezium.source.database.dbname=orderdb
debezium.source.topic.prefix=order
debezium.source.table.include.list=public.outbox
debezium.source.plugin.name=pgoutput

# Offset storage - file-based for persistence across restarts
debezium.source.offset.storage=org.apache.kafka.connect.storage.FileOffsetBackingStore
debezium.source.offset.storage.file.filename=/debezium/data/offsets.dat
debezium.source.offset.flush.interval.ms=0

# Outbox Event Router - routes outbox table changes as domain events
debezium.transforms=outbox
debezium.transforms.outbox.type=io.debezium.transforms.outbox.EventRouter
debezium.transforms.outbox.table.expand.json.payload=true
debezium.transforms.outbox.route.topic.replacement=order.events
debezium.transforms.outbox.table.field.event.timestamp=created_at

# Logging
quarkus.log.console.json=false
